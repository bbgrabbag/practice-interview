/**
 *
 * @param {String} str
 * @returns {String}
 * @summary - Given a string return a string with the characters sorted. Assume no special characters or spaces
 * @example
 *   'test' --> 'estt'
 *   '' --> ''
 *   'hello' --> 'ehllo'
 */
const sortCharacters = (str) => {
  // CODE HERE
};

/**
 * @param {String} str
 * @returns {String[]}
 * @summary - given a string, return an array of letters that occurs most often. Assume no special characters or spaces
 * @example
 *   'abc' --> ['a']
 *   '' --> []
 *   'aabb --> ['a','b']
 */
const commonLetter = (str) => {
  // CODE HERE
};

/**
 * @param {String} str
 * @returns {String[]}
 * @summary - Given a string of open and closing parenthese, determine whether they are syntactically valid
 * @example
 *   '()' --> true
 *   '())' --> false
 *   '' --> true
 *   '()()' --> true
 *   '(())())(' --> false
 *   '()((())(()))' --> true
 */
const validateBrackets = (str) => {
  // CODE HERE
};

module.exports = {
  sortCharacters,
  commonLetter,
  validateBrackets,
};
